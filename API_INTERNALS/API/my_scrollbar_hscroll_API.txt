================================================================================
  my_scrollbar_hscroll_API.txt
  Custom Win32 horizontal scrollbar — implementation notes
  my_scrollbar_hscroll.h / my_scrollbar_hscroll.cpp
================================================================================


1. PURPOSE
──────────
  Isolated horizontal-scroll delivery for all controls (ListView, TreeView,
  RichEdit).  Rewritten from scratch so H-scroll can be improved without any
  risk of disturbing the working vertical scrollbar.

  Architecture: my_scrollbar_hscroll.cpp is #included directly inside
  my_scrollbar_vscroll.cpp.  It is NOT a separate compilation unit.
  It has full access to MsbCtx, S(), Msb_FixListViewHScrollInfo, and all
  other internal helpers from my_scrollbar_vscroll.cpp.

  All H-scroll calls from Msb_ScrollToPos route through MsbH_DeliverScroll
  (defined in my_scrollbar_hscroll.cpp), regardless of control type.


2. INCLUDE
──────────
  Files that attach an H-bar include BOTH headers:

    #include "my_scrollbar_vscroll.h"   // full API declaration
    #include "my_scrollbar_hscroll.h"   // documents intent; pulls vscroll.h

  Files that only use V-scroll include only my_scrollbar_vscroll.h.


3. CURRENT STATUS — Step 2 COMPLETE: tilt-wheel works both directions (left + right)
────────────────────────────────────────────────────────────────────
  MsbH_DeliverScroll uses SetScrollInfo(nPos=lvHPos, FALSE) to pre-seed
  the counter baseline, then LVM_SCROLL(delta) inside inHDeliver=TRUE.
  Msb_HideNativeBar(H) clears WS_HSCROLL via SetWindowLongPtr instead of
  ShowScrollBar(FALSE) — preserving the internal counter between ticks.
  Tilt-wheel LEFT and RIGHT both work on ListView, TreeView, and RichEdit.

  The custom H-bar widget is NOT yet shown (MSB_HORIZONTAL visibility guards
  still in place in Msb_UpdateVisibility / Msb_UpdateVisibilityGuarded /
  WM_MOUSEMOVE proximity-expand).  No H-bar appears anywhere.

  Steps:

    Step 1 (done)    — Suppress all H-scroll.  Native bar hidden.
                       No crashes.  Baseline verified.

    Step 2 (done)    — LVM_SCROLL tilt-wheel: both directions work.
                       Root cause was ShowScrollBar(FALSE) zeroing the
                       internal scroll counter between ticks.
                       Fix: SetWindowLongPtr(~WS_HSCROLL) instead of
                       ShowScrollBar(FALSE) in Msb_HideNativeBar(H).
                       Counter persists at lvHPos between wheel ticks.

    Step 3 (next)    — Show bar: remove MSB_HORIZONTAL visibility guards.

    Step 4            — Arrow buttons: wire delivery in MsbH_DeliverScroll.

    Step 5            — Track-click: confirm via Msb_ScrollToPos.

    Step 6            — Thumb drag: confirm Golden Rules 1-3 hold.


4. GOLDEN RULES FOR LISTVIEW H-AXIS
─────────────────────────────────────
  1. lvHPos is AUTHORITATIVE.  Only written by:
       a. WM_LBUTTONDOWN drag-start (header idle — correct ground truth)
       b. MsbH_DeliverScroll()      (sets lvHPos = clamped newPos)
       c. WM_SIZE / msb_reposition  (layout settle — header correct)
     NEVER re-read from the header mid-drag / mid-scroll.
     Reason: LVM_SCROLL fires WM_NCPAINT BEFORE the header repositions.
     Msb_GetListViewHPos() inside the scroll hot-path returns the OLD
     position → delta always computed from 0 → scroll never accumulates.

  2. LVM_SCROLL(dx, 0) is the only reliable H-scroll primitive.
     Before calling it, set SCROLLINFO.nPos = lvHPos via SetScrollInfo
     (FALSE = no-redraw; does not post WM_NCPAINT) so the left-boundary
     clamp (nPos + dx >= 0) has the correct baseline.  Also set
     inHDeliver = TRUE around the call (see Rule 4).
     HOWEVER: LVM_SCROLL's actual clamp uses an internal counter, not
     SCROLLINFO.nPos directly.  SetScrollInfo alone cannot restore that
     counter.  See Rule 4 for the full picture.

  3. WS_HSCROLL is cleared by Msb_HideNativeBar(H) between wheel ticks.
     The H-axis path uses SetWindowLongPtrW(GWL_STYLE, style & ~WS_HSCROLL)
     — NOT ShowScrollBar(FALSE).  This removes the bar visually without
     touching the internal counter.  LVM_SCROLL works regardless of
     WS_HSCROLL — it does NOT check the style bit before scrolling.

  4. LVM_SCROLL is a DELTA scroll.  It uses an internal scroll counter for
     left-boundary clamping (rejects dx if counter + dx < 0).  This counter
     is separate from SCROLLINFO.nPos.  ShowScrollBar(FALSE) zeroes BOTH
     the counter and nPos.  SetScrollInfo updates nPos but NOT the counter.

     THE FIX (v2026.05.02.10): Replace ShowScrollBar(FALSE) with
     SetWindowLongPtr(~WS_HSCROLL) in Msb_HideNativeBar for the H-axis.
     SetWindowLongPtr only removes the style bit — counter is untouched.
     Counter stays at lvHPos between ticks; lvHPos + delta >= 0 always
     passes after clamping.  Both left and right tilt-wheel now work.

     Pre-seed: SetScrollInfo(nPos=lvHPos, FALSE) is called immediately
     before LVM_SCROLL so any internal ShowScrollBar(TRUE) within
     LVM_SCROLL re-seeds the counter from the correct baseline (lvHPos).


5. REFERENCE
────────────
  my_scrollbar_hscroll_template.cpp.bak — previous H-scroll implementation
  (had the right structure but was missing the ShowScrollBar(TRUE) guard).
  Use for ideas; do NOT copy-paste directly.
