================================================================================
  scrollbar_INTERNALS.txt
  Shortcuts-page vertical scrollbar — architecture, failure modes, and rationale
================================================================================

1. OVERVIEW
───────────
The Shortcuts page (page index 2) uses a custom hidden scrollbar (my_scrollbar,
HMSB s_hMsbSc) attached to the main window — identical to the Dialogs page
pattern (s_hMsbIdlg).  There is NO native WS_VSCROLL on the main window.
SCROLLINFO is used only as an internal range tracker; the visible widget is the
MSB bar.  Scrolling is driven by WM_VSCROLL (MSB thumb drag / arrow) and
WM_MOUSEWHEEL.

A canonical abstraction is provided in shortcuts.cpp: the current scroll
offset in pixels is stored in s_scScrollOffset and exposed via:

    void SC_SetScrollOffset(int off)
    int  SC_GetScrollOffset()

SC_RefreshDesktopStrip and SC_RefreshPinStrips subtract the current offset when
positioning their controls, so those helpers are scroll-aware without needing
any extra wiring.

[Migration note — 2026-04-05]
Previously a native WS_VSCROLL was conditionally added to the main window when
contentH > viewH.  Replaced with always-attached MSB (same as Dialogs page) so
the bar fades in/out gracefully and no FRAMECHANGED flash occurs on page build.


2. SCROLLINFO SETUP  (mainwindow.cpp, SwitchPage case 2)
─────────────────────────────────────────────────────────
Called once after SC_BuildPage returns the absolute-Y of the last content row:

    s_scPageContentH = SC_BuildPage(...);   // absolute Y in main-window coords

    int pageY   = s_toolbarHeight;
    int statusH = <measured via GetWindowRect(s_hStatus)>;
    int viewH   = clientBottom - pageY - statusH;
    int contentH = s_scPageContentH - pageY + S(15);   // + 15 px bottom pad

    SCROLLINFO si = {};
    si.cbSize = sizeof(si);
    si.fMask  = SIF_RANGE | SIF_PAGE | SIF_POS | SIF_DISABLENOSCROLL;
    si.nMin   = 0;
    si.nMax   = std::max(contentH - 1, viewH);
    si.nPage  = viewH;
    si.nPos   = 0;
    SetScrollInfo(hwnd, SB_VERT, &si, FALSE);   // FALSE = no native bar

    if (!s_hMsbSc) s_hMsbSc = msb_attach(hwnd, MSB_VERTICAL);
    else            msb_sync(s_hMsbSc);
    msb_set_insets(s_hMsbSc, pageY, statusH);   // clamp bar to page area
    msb_set_edge_gap(s_hMsbSc, 4);              // 4 px inward from right edge

    maxSc (effective) = contentH - viewH = s_scPageContentH + S(15) - pageY
                        - (clientBottom - pageY - statusH)
                      = s_scPageContentH + S(15) - clientBottom + statusH

At pos == maxSc the logical bottom of content (s_scPageContentH + S(15))
aligns exactly with the top of the status bar.


3. WHY SW_SCROLLCHILDREN DOES NOT WORK
────────────────────────────────────────
ScrollWindowEx(hwnd, 0, -dy, prcScroll, ..., SW_SCROLLCHILDREN) moves only
child windows that **intersect** prcScroll at call time.

Problem: controls created by SC_BuildPage start at their absolute-Y positions.
When the page is taller than the viewport, some controls start BELOW the clip
rectangle {0, pageY, width, clientBottom-statusH}.  SW_SCROLLCHILDREN never
touches them — they stay at their initial Y and are permanently unreachable.

The status bar also sits exactly at the clip-rect boundary; depending on the
window's DPI and rounding it could be caught and moved down one pixel each
scroll step, corrupting its position.


4. CORRECT SCROLL MECHANISM
─────────────────────────────
Both WM_MOUSEWHEEL and WM_VSCROLL enumerate every direct child of hwnd and
call SetWindowPos on each page control individually.  The MSB bar window itself
is excluded from the loop (it must not be scrolled):

    HWND hMsbBar = s_hMsbSc ? msb_get_bar_hwnd(s_hMsbSc) : NULL;
    HWND hC = GetWindow(hwnd, GW_CHILD);
    while (hC) {
        HWND hN = GetWindow(hC, GW_HWNDNEXT);
        if (hC != s_hStatus && hC != s_hAboutButton && hC != hMsbBar) {
            int cid = GetDlgCtrlID(hC);
            bool isTB = (cid >= IDC_TB_FILES && cid <= IDC_TB_ABOUT) ||
                         cid == IDC_TB_DIALOGS || cid == IDC_TB_COMPONENTS ||
                         cid == IDC_TB_EXIT    || cid == IDC_TB_CLOSE_PROJECT;
            if (!isTB) {
                RECT rcC; GetWindowRect(hC, &rcC);
                MapWindowPoints(HWND_DESKTOP, hwnd, (POINT*)&rcC, 2);
                SetWindowPos(hC, NULL, rcC.left, rcC.top - dy, 0, 0,
                    SWP_NOZORDER | SWP_NOSIZE | SWP_NOACTIVATE);
            }
        }
        hC = hN;
    }

Enumeration includes controls that are CURRENTLY above, within, or below the
visible page area, so every control is repositioned correctly regardless of
where it started.

After moving children the status bar is restored to HWND_TOP so it paints last
and visually covers any page control whose window rect has scrolled into the
status bar zone:

    SetWindowPos(s_hStatus, HWND_TOP,
        0, clientBottom - statusH, clientWidth, statusH, SWP_NOACTIVATE);

Then the page area is invalidated:

    RECT pageRect = { 0, spY, clientWidth, clientBottom - statusH };
    InvalidateRect(hwnd, &pageRect, TRUE);
    UpdateWindow(s_hStatus);

SC_SetScrollOffset(newPos) is called last so SC_RefreshDesktopStrip /
SC_RefreshPinStrips produce correctly offset Y values on the next refresh.


5. WINDOWS TO EXCLUDE FROM SCROLLING
──────────────────────────────────────
Handle          Condition                   Reason
─────────────── ─────────────────────────── ──────────────────────────────────
s_hStatus       explicitly excluded         always pinned to window bottom
s_hAboutButton  explicitly excluded         top-right corner, always visible
hMsbBar         msb_get_bar_hwnd(s_hMsbSc)  the scrollbar widget itself
toolbar buttons IDC_TB_FILES … IDC_TB_ABOUT Fixed toolbar area; moving them
                IDC_TB_DIALOGS              would corrupt the toolbar layout.
                IDC_TB_COMPONENTS
                IDC_TB_EXIT
                IDC_TB_CLOSE_PROJECT

All other direct children of hwnd while page 2 is active are page controls
created by SC_BuildPage and are safe to move.


6. TEARDOWN  (SwitchPage in mainwindow.cpp + SC_TearDown in shortcuts.cpp)
────────────────────────────────────────────────────────────────────────────
SwitchPage teardown (before DestroyWindow loop):

    if (s_hMsbSc) { msb_detach(s_hMsbSc); s_hMsbSc = NULL; }

SC_TearDown (shortcuts.cpp) destroys strip controls and resets offset:

    DestroyWindow(DSK_STRIP)      — desktop strip control
    DestroyWindow(SMPIN_STRIP)    — Start-menu pin strip
    DestroyWindow(TBPIN_STRIP)    — Taskbar pin strip
    s_scScrollOffset = 0          — reset logical offset

No WS_VSCROLL removal is needed (no native bar was added).


7. SC_BUILDPAGE RETURN VALUE
──────────────────────────────
SC_BuildPage returns int — the absolute Y coordinate (main-window client space)
of the first pixel below the last control row, including any trailing padding
accumulated inside SC_BuildPage.

Callers store this in mainwindow.cpp: static int s_scPageContentH = 0;

The extra S(15) padding added at SCROLLINFO setup time provides visual breathing
room at the bottom of the content.


8. STATUS-BAR HEIGHT
──────────────────────
Always measured dynamically:

    RECT rcSB; GetWindowRect(s_hStatus, &rcSB);
    int statusH = rcSB.bottom - rcSB.top;

Using a hardcoded constant (e.g. 25 px) is wrong: the status bar height depends
on the current font and DPI and may not match the live window height.


9. KNOWN LIMITATIONS
──────────────────────
- There is no container child window.  All Shortcuts-page controls are direct
  children of the main window (hwnd).  This means their window rectangles can
  technically extend into the status-bar zone when scrolled; the HWND_TOP /
  UpdateWindow pattern prevents visual corruption but does not prevent hit-test
  noise in that overlap strip.
- Window resize while page 2 is active does not recalculate maxSc or reposition
  page controls; a page-switch round-trip is required to resize the page
  correctly.


10. START-MENU TREEVIEW MSBs  (s_hMsbScSmTreeV / s_hMsbScSmTreeH)
────────────────────────────────────────────────────────────────────
The Shortcuts page also has a Start-Menu TreeView (IDC_SC_SM_TREE,
s_hScStartMenuTree) which gets its own pair of custom scroll bars so it
matches the style of the Registry and Files tree views.

Creation style — REQUIRED:
    WS_CHILD | WS_VISIBLE | WS_VSCROLL | WS_HSCROLL | TVS_…

WS_VSCROLL and WS_HSCROLL MUST be set even though the native bar visuals are
hidden by MSB (ShowScrollBar FALSE).  Without them, the TreeView never
populates SCROLLINFO (nMax always stays 0) and the MSB bars are permanently
blank with no scrolling.

Attach/detach wiring follows the exact same pattern as the Registry page MSBs;
see scrollbar_reopen_INTERNALS.txt "RULE FOR ADDING MSBs" (points a–i) and
the "TREEVIEW/LISTVIEW SCROLLINFO RESET BUG" section for the ShowScrollBar
nMax-reset pitfall that is already handled inside my_scrollbar.cpp.

Initial visibility (SwitchPage case 2, after msb_attach + msb_set_edge_gap):
    RECT rcT; GetClientRect(hSmTree, &rcT);
    SendMessageW(hSmTree, WM_SIZE, SIZE_RESTORED, MAKELPARAM(rcT.right, rcT.bottom));
This sends WM_SIZE through the installed subclass.  origProc(WM_SIZE) causes the
TreeView to recompute its layout (nPage = items-per-view).  The subclass then
captures that fresh SCROLLINFO, hides native bars, restores, and calls the
UNGUARDED Msb_UpdateVisibility on both axes — correct INVISIBLE/HIDDEN from page open.

TVN_ITEMEXPANDED hook (mainwindow.cpp WM_NOTIFY, after SC_OnNotify dispatch):
    if (nmhdr->idFrom == IDC_SC_SM_TREE && nmhdr->code == TVN_ITEMEXPANDED
        && (s_hMsbScSmTreeV || s_hMsbScSmTreeH)) {
        // hide native bars, restore SCROLLINFO, then:
        msb_reposition(s_hMsbScSmTreeV);
        msb_reposition(s_hMsbScSmTreeH);
    }
Required because tree expansion/collapse doesn't trigger WM_VSCROLL or WM_SIZE
on the TreeView.  Guard "(V||H)" prevents running during initial populate (before
MSBs are attached), avoiding stale-SCROLLINFO corruption.

Edge gap:
    msb_set_edge_gap(s_hMsbScSmTreeH, SM_CYEDGE + SM_CYBORDER + 6);
Applied to the H-bar so it clears the WS_EX_CLIENTEDGE border at the bottom
of the tree.  The V-bar uses the default gap (0 — hugs the right edge).


11. DEPENDENCIES PAGE LISTVIEW MSBs  (s_hMsbDepListV / s_hMsbDepListH)
────────────────────────────────────────────────────────────────────────
IDC_DEP_LIST (deps.cpp) gets V+H Custom scrollbars so the Dependencies page
matches all other pages.

Key ListView H-axis notes (see my_scrollbar_API.txt §1 status table and §4d checklist):
  • GetScrollInfo(SB_HORZ).nPos is unreliable for ListView H: ShowScrollBar(FALSE)
    zeroes it; LVM_SCROLL doesn't update it.  MSB tracks position in MsbCtx.lvHPos.
  • H scroll range (nMax/nPage) uses live column widths + client width, NOT
    GetScrollInfo, because the native bar may have zeroed nMax.  Helper:
    Msb_FixListViewHScrollInfo (called in Msb_Layout, Msb_Scroll, Msb_ScrollToPos,
    track-click, drag).
  • WM_HSCROLL(SB_THUMBPOSITION) does NOT work for absolute positioning (requires
    SCROLLINFO.nTrackPos which only the native scrollbar machinery sets); use
    LVM_SCROLL(delta) where delta = newPos - lvHPos.
  • Arrows and tilt wheel use WM_HSCROLL (which the ListView handles correctly
    when nMax is pre-restored before the send).
  • After the 2026-04-18 overhaul: ShowScrollBar+msb_sync boilerplate after
    InsertItem / DeleteAllItems / SetColumnWidth is NO LONGER NEEDED in calling
    code.  The subclass proc intercepts LVM_DELETEALLITEMS, LVM_SETCOLUMNWIDTH,
    and WM_NOTIFY(HDN_ENDTRACK) internally.  See §12 (2026-04 Overhaul) below.

Teardown — MUST come BEFORE the controlIds[] DestroyWindow loop (same rule as
all other MSB-attached controls — see scrollbar_reopen_INTERNALS.txt):
    DEP_TearDown(hwnd);   // contains msb_detach calls
    // ← controlIds[] loop follows
================================================================================


12. 2026-04-18 OVERHAUL — LISTVIEW H-AXIS INTERNALIZED SYNC
================================================================================

Root cause (pre-overhaul)
─────────────────────────
ListView H-axis used MsbCtx.lvHPos as a shadow position tracker because
GetScrollInfo(SB_HORZ).nPos is unreliable (zeroed by ShowScrollBar(FALSE);
not updated by LVM_SCROLL).  Keeping lvHPos in sync required callers to call
ShowScrollBar(FALSE)+msb_sync after every item insertion, column resize, and
content repopulate.  If any path was missed, lvHPos drifted and subsequent
LVM_SCROLL deltas were wrong — producing the "thumb snaps back when dragging
left" symptom.

Root cause of drag-left snap
─────────────────────────────
In addition to drift, the WM_SIZE handler inside the subclass proc called
Msb_GetListViewHPos() (reads the header item-0 rect) even when WM_SIZE fired
from within WM_NCPAINT→HideNativeBar.  LVM_SCROLL triggers WM_NCPAINT
synchronously BEFORE the header repositions its items, so the header rect
returned 0 at that moment.  The WM_SIZE read then set lvHPos=0, and the next
drag started from 0 regardless of actual scroll position.

Fixes applied to my_scrollbar.cpp
──────────────────────────────────
1. WM_SIZE — inNcPaint guard:
   The lvHPos header-read in the WM_SIZE handler is now skipped when
   inNcPaint=TRUE on either ctx.  Only genuine WM_SIZE events (window resize,
   column resize committed) update lvHPos; transient WM_SIZE events from
   LVM_SCROLL's internal ShowScrollBar(TRUE/FALSE) cycle do not.

2. New intercepts in Msb_TargetSubclassProc:
   LVM_DELETEALLITEMS  → sets lvHPos=0 and repaints (scroll position resets
                          when all items are deleted).
   LVM_SETCOLUMNWIDTH  → reads Msb_GetListViewHPos() after origProc returns
                          (header is settled at this point); repaints.
   WM_NOTIFY(HDN_ENDTRACK) → same as LVM_SETCOLUMNWIDTH but for user column
                          drags via the header control.

3. WM_LBUTTONDOWN drag-start — removed savedLvHPos header snapshot:
   The pre-Msb_PositionBar header read is no longer needed because lvHPos is
   kept authoritative by the intercepts above.  ctx->lvHPos is used directly
   as dragStartPos.

Caller impact
──────────────
The following boilerplate is NO LONGER NEEDED in any calling code:

    // OLD — no longer needed anywhere:
    ShowScrollBar(hList, SB_VERT, FALSE); msb_sync(s_hMsbListV);
    ShowScrollBar(hList, SB_HORZ, FALSE); msb_sync(s_hMsbListH);
    // after: ListView_DeleteAllItems, ListView_InsertItem, ListView_SetColumnWidth

    // OLD WM_NOTIFY handler — no longer needed:
    if (nmhdr->hwndFrom == ListView_GetHeader(hList) &&
        (nmhdr->code == HDN_ENDTRACKW || nmhdr->code == HDN_ENDTRACKA))
        msb_reposition(s_hMsbListH);

Removed from mainwindow.cpp: ForceRefreshListView, PopulateListView,
IDC_FILES_ADD_FILES, and the HDN_ENDTRACK WM_NOTIFY block.
Removed from deps.cpp: init, Add, Edit, Remove, LoadFromDb, and the
HDN_ENDTRACK DEP_OnNotify block.

What still requires explicit caller action
───────────────────────────────────────────
  • TVN_ITEMEXPANDED for TreeView: call msb_reposition (unchanged — the library
    does not intercept TreeView notifications).
  • msb_reposition after external SetWindowPos on the target (splitter/resize).
  • msb_sync / msb_notify_content_changed after WM_SETTEXT / EN_CHANGE on
    RichEdit (unchanged).


13. USAGE EXAMPLES — ALL CONTROL TYPES
================================================================================

GENERAL RULES (post-2026-04 overhaul)
──────────────────────────────────────
A. msb_attach() is the ONLY mandatory call at control creation — no boilerplate.
B. msb_detach() before or during WM_DESTROY of the target's parent dialog.
   The library also auto-detaches when the target receives WM_DESTROY.
C. msb_reposition() after external SetWindowPos on the target (splitter, resize)
   and after TVN_ITEMEXPANDED for TreeView.
D. msb_notify_content_changed() after EN_CHANGE in a RichEdit (invalidates the
   cached doc-width so the H-bar thumb stays accurate).
E. msb_sync() after programmatic scrolls that bypass WM_VSCROLL/WM_HSCROLL,
   or after WM_SETTEXT / EM_STREAMIN on RichEdit.
F. msb_set_edge_gap() to inset the bar from the window edge (cosmetic; typical:
   SM_CYEDGE + SM_CYBORDER + 6 for the H-bar on a bordered ListView/TreeView).

NOT needed (handled internally):
  ShowScrollBar(FALSE) calls          — done by msb_attach and subclass proc
  msb_sync after ListView_DeleteAllItems   — LVM_DELETEALLITEMS intercepted
  msb_sync after ListView_SetColumnWidth   — LVM_SETCOLUMNWIDTH intercepted
  HDN_ENDTRACK handler for ListView H-bar — WM_NOTIFY/HDN_ENDTRACK intercepted
  msb_sync after ListView_InsertItem       — WM_NCPAINT + WM_SIZE handle it

────────────────────────────────────────────────────────────────────────────────
LISTVIEW (report mode, V + H)     e.g. Files, Deps, Registry, Components
────────────────────────────────────────────────────────────────────────────────

  static HWND s_hList = NULL;
  static HMSB s_hMsbV = NULL;
  static HMSB s_hMsbH = NULL;

  // --- WM_INITDIALOG / page creation ---
  s_hList = GetDlgItem(hwnd, IDC_MY_LIST);
  s_hMsbV = msb_attach(s_hList, MSB_VERTICAL);
  s_hMsbH = msb_attach(s_hList, MSB_HORIZONTAL);
  if (s_hMsbH)
      msb_set_edge_gap(s_hMsbH, GetSystemMetrics(SM_CYEDGE)
                                + GetSystemMetrics(SM_CYBORDER) + 6);
  PopulateList();   // DeleteAllItems + InsertItem + SetColumnWidth — all handled

  // --- RefreshList / repopulate ---
  static void RefreshList() {
      ListView_DeleteAllItems(s_hList);        // intercept → lvHPos=0
      for (auto& item : s_items) { /* InsertItem ... */ }
      ListView_SetColumnWidth(s_hList, 0, LVSCW_AUTOSIZE_USEHEADER);
      ListView_SetColumnWidth(s_hList, 1, LVSCW_AUTOSIZE);
      // No ShowScrollBar + msb_sync needed.
  }

  // --- WM_NOTIFY: no HDN_ENDTRACK handler needed ---
  // Only handle LVN_* notifications.

  // --- WM_SIZE (splitter / resize) ---
  if (s_hMsbV) msb_reposition(s_hMsbV);
  if (s_hMsbH) msb_reposition(s_hMsbH);

  // --- WM_DESTROY ---
  msb_detach(s_hMsbV); s_hMsbV = NULL;
  msb_detach(s_hMsbH); s_hMsbH = NULL;

────────────────────────────────────────────────────────────────────────────────
TREEVIEW (V + H)     e.g. Files tree, Components tree, Registry tree
────────────────────────────────────────────────────────────────────────────────
  REQUIRED: create with WS_VSCROLL | WS_HSCROLL even though native bars are
  hidden.  Without these styles, the TreeView never populates SCROLLINFO and
  MSB bars stay blank.

  static HWND s_hTree    = NULL;
  static HMSB s_hMsbTreeV = NULL;
  static HMSB s_hMsbTreeH = NULL;

  // --- WM_INITDIALOG ---
  s_hTree    = GetDlgItem(hwnd, IDC_MY_TREE);
  s_hMsbTreeV = msb_attach(s_hTree, MSB_VERTICAL);
  s_hMsbTreeH = msb_attach(s_hTree, MSB_HORIZONTAL);
  if (s_hMsbTreeH)
      msb_set_edge_gap(s_hMsbTreeH, GetSystemMetrics(SM_CYEDGE)
                                    + GetSystemMetrics(SM_CYBORDER) + 6);
  PopulateTree(); // TreeView_InsertItem calls

  // Send WM_SIZE through subclass so nPage is initialised correctly:
  RECT rcT; GetClientRect(s_hTree, &rcT);
  SendMessageW(s_hTree, WM_SIZE, SIZE_RESTORED,
               MAKELPARAM(rcT.right, rcT.bottom));

  // --- WM_NOTIFY: TVN_ITEMEXPANDED (REQUIRED for TreeView!) ---
  // The library does not intercept tree notifications — you must call
  // msb_reposition after expand/collapse so the V-bar updates row count.
  if (nmhdr->hwndFrom == s_hTree &&
      (nmhdr->code == TVN_ITEMEXPANDED || nmhdr->code == TVN_ITEMEXPANDEDW)) {
      if (s_hMsbTreeV) msb_reposition(s_hMsbTreeV);
      if (s_hMsbTreeH) msb_reposition(s_hMsbTreeH);
  }

  // --- WM_SIZE ---
  if (s_hMsbTreeV) msb_reposition(s_hMsbTreeV);
  if (s_hMsbTreeH) msb_reposition(s_hMsbTreeH);

  // --- WM_DESTROY ---
  msb_detach(s_hMsbTreeV); s_hMsbTreeV = NULL;
  msb_detach(s_hMsbTreeH); s_hMsbTreeH = NULL;

────────────────────────────────────────────────────────────────────────────────
RICHEDIT — READ-WRITE editor (V + H, no-wrap)     e.g. Scripts editor
────────────────────────────────────────────────────────────────────────────────

  static HWND s_hEdit    = NULL;
  static HMSB s_hMsbEditV = NULL;
  static HMSB s_hMsbEditH = NULL;

  // --- WM_INITDIALOG ---
  s_hEdit = GetDlgItem(hwnd, IDC_SCRIPT_EDIT);
  // Disable word-wrap BEFORE attaching so H bar measures correctly:
  SendMessageW(s_hEdit, EM_SETTARGETDEVICE, (WPARAM)NULL, 1);
  s_hMsbEditV = msb_attach(s_hEdit, MSB_VERTICAL);
  s_hMsbEditH = msb_attach(s_hEdit, MSB_HORIZONTAL);

  // --- EN_CHANGE (content edited) ---
  if (s_hMsbEditH) msb_notify_content_changed(s_hMsbEditH);
  // msb_notify_content_changed also syncs the peer V bar.

  // --- Loading new content (WM_SETTEXT / EM_STREAMIN) ---
  SendMessageW(s_hEdit, WM_SETTEXT, 0, (LPARAM)newText);
  if (s_hMsbEditV) msb_sync(s_hMsbEditV);
  if (s_hMsbEditH) msb_notify_content_changed(s_hMsbEditH);

  // --- WM_SIZE ---
  if (s_hMsbEditV) msb_reposition(s_hMsbEditV);
  if (s_hMsbEditH) msb_reposition(s_hMsbEditH);

  // --- WM_DESTROY ---
  msb_detach(s_hMsbEditV); s_hMsbEditV = NULL;
  msb_detach(s_hMsbEditH); s_hMsbEditH = NULL;

────────────────────────────────────────────────────────────────────────────────
RICHEDIT — READ-ONLY viewer (V + H)     e.g. About, Changelog, License
────────────────────────────────────────────────────────────────────────────────
  Identical to read-write above, with these differences:
    SendMessageW(s_hView, EM_SETREADONLY, TRUE, 0);
  No EN_CHANGE handler needed.  Call msb_sync + msb_notify_content_changed
  once after loading content.

  Wrap-mode viewer (V only):
    s_hMsbViewV = msb_attach(s_hView, MSB_VERTICAL);
    // Do not attach MSB_HORIZONTAL — no H bar needed when wrapped.

────────────────────────────────────────────────────────────────────────────────
DIALOGS EDITOR (multi-line no-wrap RichEdit, V + H, with toolbar insets)
────────────────────────────────────────────────────────────────────────────────
  Same as read-write RichEdit.  Add insets so bars avoid chrome:

  s_hMsbEditV = msb_attach(s_hEdit, MSB_VERTICAL);
  s_hMsbEditH = msb_attach(s_hEdit, MSB_HORIZONTAL);
  msb_set_insets(s_hMsbEditV, toolbarHeight, statusBarHeight);
  msb_set_insets(s_hMsbEditH, 0, statusBarHeight);

────────────────────────────────────────────────────────────────────────────────
MAIN WINDOW / TOP-LEVEL (V only, toolbar + status insets)
────────────────────────────────────────────────────────────────────────────────
  // Bar is created as a child of the target itself (no parent).
  static HMSB s_hMsbMain = NULL;

  // WM_CREATE / after layout:
  s_hMsbMain = msb_attach(hwnd, MSB_VERTICAL);
  msb_set_insets(s_hMsbMain, TOOLBAR_HEIGHT, STATUSBAR_HEIGHT);
  msb_set_edge_gap(s_hMsbMain, 4);

  // Exclude bar from child-moving scroll loops:
  HWND hBarWnd = msb_get_bar_hwnd(s_hMsbMain);
  HWND hChild = GetWindow(hwnd, GW_CHILD);
  while (hChild) {
      if (hChild != hBarWnd)
          SetWindowPos(hChild, NULL, newX, newY, 0, 0, SWP_NOSIZE|SWP_NOZORDER);
      hChild = GetWindow(hChild, GW_HWNDNEXT);
  }

  // WM_SIZE: if (s_hMsbMain) msb_reposition(s_hMsbMain);
  // WM_DESTROY: msb_detach(s_hMsbMain); s_hMsbMain = NULL;

────────────────────────────────────────────────────────────────────────────────
ALWAYS-VISIBLE BARS (MSB_NOHIDE)
────────────────────────────────────────────────────────────────────────────────
  // No fade animation — bar stays at full width at all times.
  s_hMsbV = msb_attach(hTarget, MSB_VERTICAL   | MSB_NOHIDE);
  s_hMsbH = msb_attach(hTarget, MSB_HORIZONTAL | MSB_NOHIDE);
  // All other patterns identical to auto-hide variants.

────────────────────────────────────────────────────────────────────────────────
TEARDOWN ORDER RULE
────────────────────────────────────────────────────────────────────────────────
  Always msb_detach BEFORE DestroyWindow on the target, or in WM_DESTROY of
  the parent dialog BEFORE chaining to DefDlgProc.  Never detach after the
  target is destroyed.

  Recommended:
  case WM_DESTROY:
      msb_detach(s_hMsbV); s_hMsbV = NULL;
      msb_detach(s_hMsbH); s_hMsbH = NULL;
      // ... other cleanup ...
      return 0;

────────────────────────────────────────────────────────────────────────────────
QUICK REFERENCE CARD
────────────────────────────────────────────────────────────────────────────────
  Function                         When to call
  ──────────────────────────────────────────────────────────────────────────────
  msb_attach(hWnd, flags)          Once per axis at control creation.
  msb_detach(h)                    At WM_DESTROY of target or its parent dialog.
  msb_reposition(h)                After external SetWindowPos on target, and
                                   after TVN_ITEMEXPANDED for TreeView.
  msb_sync(h)                      After programmatic scroll bypassing
                                   WM_VSCROLL/WM_HSCROLL, or after
                                   WM_SETTEXT/EM_STREAMIN on RichEdit.
  msb_notify_content_changed(h)    After EN_CHANGE or content edit in RichEdit.
  msb_set_insets(h, near, far)     After attach when bar must avoid chrome.
  msb_set_edge_gap(h, gap)         After attach for cosmetic inset from edge.
  msb_get_bar_hwnd(h)              To exclude bar from child-enumeration loops.
================================================================================
