================================================================================
  Session State — SetupCraft
  Version: 2026.03.26.16  |  Published: 26.03.2026 16:32
  Commit: (see git log — pushed end of session 26.03.2026)
================================================================================

------------------------------------------------------------------------
COMPLETED THIS SESSION (26.03.2026)
------------------------------------------------------------------------

 [OK]  Preview — developer-sized lock:
         - s_previewUserSized flag (bool, module-level)
         - Set to true on first EN_CHANGE in sizer panel (any spin/type)
         - Once true: all AutoFitComponentHeight calls are skipped
         - Reset to false in IDLG_Reset() on project switch / new project
         - Persisted: installer_preview_user_sized_<id> in settings table
           (saved in IDLG_SaveToDb, loaded in IDLG_LoadFromDb)

 [OK]  Preview — smart auto-fit for IDLG_COMPONENTS:
         - AutoFitComponentHeight() now handles both layouts:
           a) contentHidden=true  (no RTF): 144 + n*28 logical px
           b) contentHidden=false (split):  max(2*rtfH+124, n*56+190)
         - MeasureRichEditLogHeight(HWND hRE) helper added:
             Uses EM_FORMATRANGE (measure-only, wParam=FALSE) on a very
             tall rect; fr.rc.top after the call = content extent in twips
             Converts via MulDiv(twips, 96, 1440) -> logical pixels
         - Called from both NavigateTo (page switch) and ShowPreviewDialog
           (initial open); skipped if s_previewUserSized is true

 [OK]  Preview — app comes to front on close:
         - Root cause: EnableWindow(hwndParent, TRUE) was called AFTER
           DestroyWindow; Windows had already activated another window
         - Fix: WM_DESTROY on the preview re-enables the owner immediately
           (before Windows picks a new foreground window)
         - ShowPreviewDialog then calls SetActiveWindow + SetForegroundWindow
           as belt-and-suspenders after the loop; EnableWindow is no-op
           if already enabled

------------------------------------------------------------------------
KNOWN ISSUES / TODO NEXT
------------------------------------------------------------------------

 [!!]  AutoFitComponentHeight — formula still not correct for images:
         The dialog resizes but the RTF content (e.g. an embedded logo
         image) is still clipped; the RichEdit area is too short to show
         the full image without scrolling.
         - Window grows from its top-left corner (SWP_NOMOVE) rather than
           re-centering after a size increase
         - Suspect: EM_FORMATRANGE measurement is off when the RichEdit
           has not been fully laid out at measurement time (window not yet
           shown, or GetClientRect returns stale size from a prior pass)
         - Possible fixes to try next session:
             1. Force a layout pass (SetWindowPos + UpdateWindow on the
                preview window) before calling EM_FORMATRANGE
             2. After resize, re-center the preview on the parent window
             3. Use EM_GETLINECOUNT x line-height as an alternative measure

========================================================================
